usage_options="USAGE:
    $op_sh [ -c | [-f] [--dry-run] ] [host [host]...]
OPTIONS:
    -c         clean instead of deploy
    -f         force
    --dry-run  dry run
    host       host to deploy to"
